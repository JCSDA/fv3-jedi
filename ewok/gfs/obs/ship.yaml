obs space:
  name: Ship
  obsdatain:
    obsfile: $(experiment_dir)/{{current_cycle}}/.ship.$(date).nc4
  obsdataout:
    obsfile: $(experiment_dir)/{{current_cycle}}/$(experiment).ship.$(date).nc4
  simulated variables:
    - surface_pressure
obs operator:
  name: SfcPCorrected
  da_psfc_scheme: UKMO
