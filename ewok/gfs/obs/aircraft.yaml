obs space:
  name: Aircraft
  obsdatain:
    obsfile: $(experiment_dir)/{{current_cycle}}/aircraft.$(date).nc4
  obsdataout:
    obsfile: $(experiment_dir)/{{current_cycle}}/$(experiment).aircraft.$(date).nc4
  simulated variables:
    - eastward_wind
    - northward_wind
    - air_temperature
    - specific_humidity
obs operator:
  name: VertInterp
