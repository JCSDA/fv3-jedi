forecast length:
  $(forecast_length)
geometry:
  $(GEOMETRY)
forecasts:
  $(BACKGROUND)
model:
  $(MODEL)
observations:
  $(OBSERVATIONS)
window begin: $(window begin)
window length: $(window length)
